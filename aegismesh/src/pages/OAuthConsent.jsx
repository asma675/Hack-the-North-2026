import { Link } from 'react-router-dom';
export default function OAuthConsent(){return <div className="min-h-screen grid place-items-center bg-background p-6"><div className="aegis-panel max-w-lg p-8"><h1 className="text-xl font-bold">Connector authorization</h1><p className="text-sm text-muted-foreground mt-3">The standalone AegisMesh build uses its own API and bearer-token authentication. External connector OAuth can be added behind the backend without exposing credentials in the browser.</p><Link to="/app" className="inline-flex mt-5 text-primary hover:underline">Return to AegisMesh →</Link></div></div>}
